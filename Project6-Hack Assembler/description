The hack assembler(written in C++) takes a file *.asm and converts it into a *.hack file(machine code) where each instruction is 16-bits.
The input and output files have to be set inside the c++ program and just run it to generate the *.hack file.
